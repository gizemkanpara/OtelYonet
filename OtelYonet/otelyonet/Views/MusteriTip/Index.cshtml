@model IEnumerable<otelyonet.Models.MusteriTip>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MusteriTipleri)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MusteriTipleri)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MusteriTipID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MusteriTipID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MusteriTipID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
